const express = require('express');
const orderRoutes = require('./orderRoutes');

const router = express.Router();

// Attach individual route files
router.use('/orders', orderRoutes);

module.exports = router;
