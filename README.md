# Loger MVP (Push scenario)

## Quick start (local)
1. Install Node 18+ and MongoDB locally (or use MongoDB Atlas).
2. Copy `.env.example` -> `.env` and set `MONGODB_URI`.
3. Install deps:
   ```bash
   npm install
